export class Ingredient {
    //This setup automatically gives us properties with the names provided and asign then values in the constructor
    constructor (public name: string, public amount: number) {}
}